# Dependencias y compilados
node_modules
dist
functions/node_modules
functions/lib
*.tsbuildinfo

# Secretos y configuración local
.env
.env.local
.firebase

# Respaldos que crea INSTALAR_ACTUALIZACION.bat
_respaldo_*/

# Paquetes de actualización: no tienen por qué vivir en el repositorio
*.zip

# Sistema
*.log
.DS_Store
Thumbs.db

# Datos de negocio: nunca en el repositorio, y menos en public/
# (todo lo que vive en public/ se publica en Hosting sin pedir contrasena)
public/*.json
!public/manifest.json
contrarecibos.json
facturas_pendientes_contrarecibo.json
